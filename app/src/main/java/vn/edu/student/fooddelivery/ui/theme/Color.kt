package vn.edu.student.fooddelivery.ui.theme

import androidx.compose.ui.graphics.Color

// -------- Bảng màu chính (tông cam ấm) ---------------
val Orange40 = Color(0xFFE8632A)   // Primary — nút CTA, icon nhấn mạnh
val Orange80 = Color(0xFFFFB088)   // Primary nhạt — nền badge, hover nhẹ
val OrangeContainer = Color(0xFFFFE4D6)

val Neutral10 = Color(0xFF1C1B1A)  // Text primary
val Neutral40 = Color(0xFF6F6C68)  // Text secondary
val Neutral90 = Color(0xFFF5F3F1)  // Background
val Neutral99 = Color(0xFFFFFFFF)  // Surface (card, input)

// ---- Màu trạng thái đơn hàng (dùng cho StatusBadge) ----
val StatusPending = Color(0xFF616161)      // xám — chưa ai nhận
val StatusAccepted = Color(0xFF1565C0)     // xanh dương — shipper đã nhận
val StatusPickedUp = Color(0xFF6A1B9A)     // tím — đã lấy hàng
val StatusInTransit = Color(0xFF8A5A00)    // hổ phách đậm — đang giao
val StatusDelivered = Color(0xFF1B5E20)    // xanh lá — hoàn thành
val StatusCancelled = Color(0xFFB3261E)    // đỏ — đã huỷ
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
